<footer>
    <nav class="pagination" role="navigation">
        {{#if prev}}
        <a class="page previous newer-posts" href="{{page_url prev}}">Artigos novos</a>
        {{/if}}
        <span class="page-number">Page {{page}} of {{pages}}</span>
        {{#if next}}
        <a class="page next older-posts" href="{{page_url next}}">Artigos antigos</a>
        {{/if}}
    </nav>
</footer>